import XCTest
@testable import SwiftTURN

class SwiftTURNTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftTURN().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
